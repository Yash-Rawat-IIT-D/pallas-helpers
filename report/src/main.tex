\ifdefined\HANDOUTMODE
  \PassOptionsToClass{handout}{beamer}
\fi
\documentclass[10pt,aspectratio=169]{beamer}
\usetheme{Warsaw}
\usepackage{graphicx}

\title{Pallas Storage Compression Report}
\author{Yash Rawat}
\institute{Telecom SudParis, France}
\date{\today}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Overview}
  \begin{itemize}
    \item<1-> Motivation: reduce the storage cost of redundant timing data in Pallas traces without compromising the usefulness of the recorded execution behavior.
    \item<2-> Trace shaping: improved loop and sequence detection so repeated polling patterns can be represented more compactly.
    \item<3-> Lossless encoding: evaluated timestamp and duration compression schemes that preserve exact values while reducing trace size.
    \item<4-> Lossy encoding: introduced controlled approximation paths for highly redundant timing vectors to study compression-quality tradeoffs.
    \item<5-> Benchmarking: built write-time, read-time, verification, and post-mortem analysis tooling to quantify overheads and compression gains.
  \end{itemize}
\end{frame}

\begin{frame}[fragile]{Motivation}
  \begin{columns}[T]
    \begin{column}{0.5\textwidth}
      \begin{block}{Spinloop Pattern}
\small
\begin{verbatim}
for iter = 1 .. iterations:
  MPI_Barrier(comm)

  if rank == receiver:
    MPI_Irecv(..., &req)
    flag = 0
    while !flag:
      MPI_Test(&req, &flag, IGNORE)
  else:
    sleep(delay_us)
    MPI_Isend(..., &req)
    MPI_Wait(&req, IGNORE)
\end{verbatim}
      \end{block}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{itemize}
        \item<2-> Each delayed send forces the receiver into a hot polling loop with many failed \texttt{MPI\_Test} calls before one final success.
        \item<3-> Across large iteration counts, this generates massive amounts of repetitive timing data with very similar sequence structure.
        \item<4-> The resulting Pallas traces are often dominated by timestamp and duration vectors attached to these repeated polling sequences.
        \item<5-> Our goal is to compress this redundancy while preserving the execution information needed for post-mortem analysis.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Supporting Data}
  \small
  \begin{block}{Spinloop Benchmark Cases}
    \centering
    \begin{tabular}{|c|c|c|}
      \hline
      Iterations & Delay ($\mu$s) & Trace Directory Size (in MiB) \\
      \hline
      1000 & 10000 & \textit{11} \\
      \hline
      10000 & 10000 & \textit{91} \\
      \hline
      1000 & 50000 & \textit{fill} \\
      \hline
      10000 & 50000 & \textit{fill} \\
      \hline
    \end{tabular}
  \end{block}

  \vspace{0.5em}
  \begin{itemize}
    \item For the spinloop benchmark, the dominant contribution to trace size comes from the receiver-side polling thread, and more specifically from the timestamp and duration vectors associated with repeated failed \texttt{MPI\_Test} calls.
    \item Although these failed polling attempts are numerous, they are of limited analytical value in most post-mortem workflows: they mainly record that the request had not yet completed, while contributing heavily to the overall storage footprint.
    \item For the case with 10{,}000 iterations and a 10{,}000~\textmu s delay, the raw timing data associated with this behavior is about 401.81~MB; after the existing ZSTD backend it still occupies about 84.13~MB, corresponding to a compression ratio of roughly 4.77.
    \item Our compression work is therefore targeted at this specific redundancy pattern: preserve the useful temporal behavior, while reducing the storage cost of high-volume failed polling data.
  \end{itemize}
\end{frame}

\begin{frame}{Earlier Pallas Storage Path}
  \small
  \begin{block}{Baseline write-side flow before subarray-specific encoding}
    \centering
    \texttt{ThreadWriter / instrumentation} \\
    $\downarrow$ \\
    \texttt{LinkedVector::add(...)} / \texttt{LinkedDurationVector::add(...)} \\
    $\downarrow$ \\
    \texttt{storeThread(...)} \\
    $\downarrow$ \\
    \texttt{storeEvent(...)} / \texttt{storeSequence(...)} \\
    $\downarrow$ \\
    \texttt{LinkedVector::write\_to\_file(...)} / \texttt{LinkedDurationVector::write\_to\_file(...)} \\
    $\downarrow$ \\
    raw subarray values written to archive files
  \end{block}

  \begin{itemize}
    \item<2-> During instrumentation, timestamps and durations are appended through \texttt{add(...)} into linked vectors that are later flushed to disk.
    \item<3-> In \texttt{pallas\_storage.cpp}, \texttt{storeThread(...)} iterates over the thread content and delegates timing-vector storage to \texttt{storeEvent(...)} and \texttt{storeSequence(...)}.
    \item<4-> Historically, this path mostly treated each subarray as raw timing data that would be passed to the generic file compression stage.
    \item<5-> The new subarray encoding work does not replace the overall archive layout; it inserts an extra transformation step inside this existing write path.
  \end{itemize}
\end{frame}

\begin{frame}{SubArrayEncoding and SubArrayCodec}
  \small
  \begin{itemize}
    \item<1-> The new \texttt{enum class SubArrayEncoding} in \texttt{pallas\_linked\_vector.h} defines the per-subarray encoding policy: \texttt{None}, \texttt{Delta2VintTimestamp}, \texttt{Delta2VintDuration}, \texttt{MonotoneLossy}, and \texttt{DurationLossy}.
    \item<2-> Each \texttt{LinkedVector::SubArray} or \texttt{LinkedDurationVector::SubArray} stores its own \texttt{sub\_arr\_encoding}, so the choice is made at subarray granularity rather than once for the whole vector.
    \item<3-> The abstract base class \texttt{SubArrayCodec} provides the interface through \texttt{can\_encode(...)}, \texttt{encode(...)}, and \texttt{decode(...)}.
    \item<4-> Concrete implementations such as \texttt{NoneCodec}, \texttt{TimestampDelta2VintCodec}, \texttt{DurationDelta2VintCodec}, \texttt{MonotoneLossyCodec}, and \texttt{DurationLossyCodec} are selected by \texttt{get\_subarray\_codec(...)}.
    \item<5-> The output of \texttt{encode(...)} is an \texttt{encodedArray} and its logical size \texttt{enc\_size}, which is exactly the quantity that influences the downstream storage footprint.
  \end{itemize}
\end{frame}

\begin{frame}{Where the Codec Enters the Storage Path}
  \small
  \begin{block}{Write-side flow}
    \centering
    \texttt{storeEvent(...)} / \texttt{storeSequence(...)} \\
    $\downarrow$ \\
    \texttt{timestamps/durations->write\_to\_file(...)} \\
    $\downarrow$ \\
    \texttt{SubArray::write\_to\_file(...)} \\
    $\downarrow$ \\
    \texttt{get\_subarray\_codec(sub\_arr\_encoding)} \\
    $\downarrow$ \\
    \texttt{codec->encode(...)} $\rightarrow$ \texttt{enc\_size} \\
    $\downarrow$ \\
    \texttt{\_pallas\_compress\_write(encodedArray, enc\_size, ...)}
  \end{block}

  \begin{itemize}
    \item<2-> In \texttt{pallas\_storage.cpp}, \texttt{storeEvent(...)} and \texttt{storeSequence(...)} are the earlier entry points that push timing vectors toward archive storage.
    \item<3-> The encoding hook itself is in \texttt{LinkedVector::SubArray::write\_to\_file(...)} and \texttt{LinkedDurationVector::SubArray::write\_to\_file(...)}.
    \item<4-> If \texttt{codec->can\_encode(...)} fails, the subarray falls back to \texttt{SubArrayEncoding::None}; otherwise \texttt{codec->encode(...)} reduces or reshapes the logical payload before file compression.
    \item<5-> This means the codec affects size twice: first through the encoded representation size \texttt{enc\_size}, and then again through how well that representation compresses in \texttt{\_pallas\_compress\_write(...)}.
    \item<6-> On the read path, \texttt{LinkedVector::load\_data(...)} and \texttt{LinkedDurationVector::load\_data(...)} reconstruct values using the matching \texttt{codec->decode(...)} implementation.
  \end{itemize}
\end{frame}

\begin{frame}{Delta2Vint Codec: General Idea}
  \small
  \begin{itemize}
    \item<1-> \texttt{Delta2VintTimestampCodec} and \texttt{Delta2VintDurationCodec} are lossless codecs: they preserve the exact original values after decoding.
    \item<2-> Instead of storing every timestamp or duration as a full 64-bit integer, the codec stores a base value followed by deltas, and then mostly delta-of-delta values.
    \item<3-> For repeated polling behavior, both timestamps and durations evolve smoothly, so these delta-of-delta values are often small in magnitude.
    \item<4-> Smaller logical values reduce the raw encoded footprint before generic compression, which is why we expect \texttt{raw} bytes to drop relative to \texttt{pre\_raw} bytes.
    \item<5-> This also helps ZSTD because it receives a denser and more structured byte stream after \texttt{encode(...)}, improving the final compressed size without losing exactness.
  \end{itemize}
\end{frame}

\begin{frame}{Delta2Vint Codec: LEB128 and ZigZag}
  \scriptsize
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \begin{itemize}\setlength{\itemsep}{0.35em}
        \item<1-> The implementation uses a variable-length integer representation through \texttt{write\_varint(...)} and \texttt{read\_varint(...)}, which is effectively a LEB128-style byte encoding.
        \item<2-> Small values occupy fewer bytes, so when delta-of-delta terms stay near zero, the encoded stream becomes much smaller than a flat array of 64-bit words.
        \item<3-> Because delta-of-delta values can be negative, the codec applies ZigZag conversion through \texttt{zigzag\_encode(...)} and \texttt{zigzag\_decode(...)} before writing the varints.
        \item<4-> The timestamp path assumes non-negative first-order deltas, while the duration path allows the first delta itself to be signed; this is the main implementation difference between the two variants.
        \item<5-> The result is still fully lossless: the decoder reconstructs the same sequence of timestamps or durations from the compact byte representation.
      \end{itemize}
    \end{column}
    \begin{column}{0.75\textwidth}
      \centering
      \includegraphics[width=\linewidth,height=0.75\textheight,keepaspectratio]{../images/broll/vint.png}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Lossy Encoding: Modular Design}
  \small
  \begin{itemize}
    \item<1-> Beyond lossless compression, Pallas now exposes lossy subarray encodings through \texttt{SubArrayEncoding::MonotoneLossy} and \texttt{SubArrayEncoding::DurationLossy}.
    \item<2-> The current implementation is modular: different predictor families are represented explicitly through \texttt{MonotoneLossyVariant} and \texttt{DurationLossyVariant}.
    \item<3-> For timestamps, the current branch already distinguishes multiple monotone variants such as \texttt{QLinear}, \texttt{QLinearMeanRep}, and related extensions.
    \item<4-> For durations, the new \texttt{DurationLossy} path introduces variants such as \texttt{QLinear} and \texttt{QLinearMeanRep}, with the same goal of keeping the codec pluggable.
    \item<5-> The configuration layer was extended accordingly: both the general subarray encoding scheme and the chosen lossy variant can now be selected through the Pallas config file and corresponding environment variables.
    \item<6-> This makes future codec experimentation much easier, because new lossy predictors can be integrated behind the same \texttt{encode(...)} / \texttt{decode(...)} interface with limited disruption to the storage path.
  \end{itemize}
\end{frame}

\begin{frame}{Lossy Encoding: Accuracy vs Compression}
  \small
  \begin{itemize}
    \item<1-> The current lossy predictors are based on a fixed set of percentile anchors, and the number of anchors can be varied to trade accuracy against compression ratio.
    \item<2-> With fewer \texttt{kKPercentileAnchorCount} anchor values, the encoded representation becomes smaller, but the reconstructed signal is coarser.
    \item<3-> With more anchors, the reconstructed timestamps or durations better follow the original shape, at the cost of storing more predictor state.
    \item<4-> This is especially useful for highly repetitive polling traces, where approximate reconstruction can remove substantial redundancy while preserving the overall temporal behavior.
    \item<5-> The benchmark and verification work added alongside these codecs gives us a way to measure that tradeoff through write cost, read cost, compression ratio, and error metrics.
    \item<6-> Future work over the coming weeks will focus on stronger variants of the current \texttt{QLinear} predictor; because the implementation is modular, these new predictors can be slotted into the existing framework with relatively little effort.
  \end{itemize}
\end{frame}

\begin{frame}{Benchmark Configuration Labels}
  \small
  \begin{tabular}{|c|c|c|c|}
    \hline
    Test & TS Encoding & Duration Encoding & overrideLoopPolicy \\
    \hline
    test\_0 & \texttt{None} & \texttt{None} & \texttt{false} \\
    \hline
    test\_1 & \texttt{None} & \texttt{None} & \texttt{true} \\
    \hline
    test\_2 & \texttt{Delta2VintTimestamp} & \texttt{Delta2VintDuration} & \texttt{false} \\
    \hline
    test\_3 & \texttt{Delta2VintTimestamp} & \texttt{Delta2VintDuration} & \texttt{true} \\
    \hline
  \end{tabular}

  \vspace{0.6em}
  \begin{itemize}
    \item \texttt{test\_0} is the absolute baseline: no subarray encoding and no loop-policy override.
    \item \texttt{test\_1} isolates the effect of enabling the loop-policy override while keeping the default encoding path unchanged.
    \item \texttt{test\_2} isolates the effect of introducing lossless \texttt{Delta2Vint} encoding without changing the loop-detection policy.
    \item \texttt{test\_3} combines the two ideas: lossless \texttt{Delta2Vint} encoding together with loop-policy override.
  \end{itemize}
\end{frame}

\begin{frame}{Compression : Spinloop (1{,}000 iter, 10{,}000~\textmu s)}
  \scriptsize
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Pre-Raw & Raw & Compr. & Eff. & True \\
    \hline
    test\_0 & 46544808 & 46544808 & 9679401 & 4.81 & 4.81 \\
    \hline
    test\_1 & 46829528 & 802808 & 657985 & 1.22 & 71.17 \\
    \hline
    test\_2 & 46602888 & 6324496 & 4127874 & 1.53 & 11.29 \\
    \hline
    test\_3 & 46711688 & 605432 & 620158 & 0.98 & 75.32 \\
    \hline
  \end{tabular}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.53\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260605-102501/overall_compression.png}

  \vspace{0.2em}
  \begin{itemize}
    \item The same ordering is preserved at the smaller iteration count.
    \item This confirms the compression trend is not limited to only the largest spinloop run.
  \end{itemize}
\end{frame}

\begin{frame}{Compression : Spinloop (10{,}000 iter, 10{,}000~\textmu s)}
  \scriptsize
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Pre-Raw & Raw & Compr. & Eff. & True \\
    \hline
    test\_0 & 403370488 & 403370488 & 85625950 & 4.71 & 4.71 \\
    \hline
    test\_1 & 397777408 & 6948032 & 5994069 & 1.16 & 66.36 \\
    \hline
    test\_2 & 400057888 & 56924640 & 40786064 & 1.40 & 9.81 \\
    \hline
    test\_3 & 401940048 & 5339032 & 5692582 & 0.94 & 70.61 \\
    \hline
  \end{tabular}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.53\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260603-162313/overall_compression.png}

  \vspace{0.2em}
  \begin{itemize}
    \item \texttt{test\_0} is the baseline \texttt{None/None} configuration.
    \item The main gain comes from shrinking the logical raw representation before final compression.
  \end{itemize}
\end{frame}

\begin{frame}{Compression : Chameleon POTRF ($n=65536$, $P=4$, 16 MPI jobs)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Pre-Raw & Raw & Compr. & Eff. & True \\
    \hline
    test\_0 & 4511987856 & 4511987856 & 1054877684 & 4.28 & 4.28 \\
    \hline
    test\_1 & 4468570976 & 74899864 & 92419782 & 0.81 & 48.35 \\
    \hline
    test\_2 & 4433628136 & 852423520 & 736478532 & 1.16 & 6.02 \\
    \hline
    test\_3 & 4612159936 & 68591072 & 93429655 & 0.73 & 49.37 \\
    \hline
  \end{tabular}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.5\textheight,keepaspectratio]{../images/chameleon_stesting/run-analysis-20260604-125221/overall_compression.png}

  \vspace{0.2em}
  \texttt{mpirun -np 16 --bind-to l3cache ./chameleon\_stesting -o potrf -H --niter 1 -w -P 4 -n 65536}
\end{frame}

\begin{frame}{Write-Time Benchmark Normalization}
  \small
  \begin{itemize}
    \item<1-> Raw write times are not directly comparable across runs because each test generates a different number of timestamp and duration values.
    \item<2-> We therefore normalize the write overhead by the total number of processed values, giving a primary metric in \texttt{ns/value}.
    \item<3-> In the analysis pipeline, the reported net write overhead already excludes verification cost:
    \[
      \texttt{net\_write\_ns} = \texttt{write\_time\_ns} - \texttt{verification\_ns}.
    \]
    \item<4-> The stacked write plots decompose this normalized cost across four vector families: event timestamps, sequence timestamps, sequence durations, and sequence exclusive durations.
    \item<5-> With this normalization, the \texttt{Delta2Vint} no-override case adds only a moderate overhead relative to the \texttt{None/None} baseline, while the override-enabled cases can even become cheaper overall.
    \item<6-> Our current interpretation is that this reduction is driven by the smaller amount of logical data emitted by the transformed trace representation, which reduces the downstream storage workload.
  \end{itemize}
\end{frame}

\begin{frame}{Write Time : Spinloop (1{,}000 iter, 10{,}000~\textmu s)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Total & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 76.06 & 33.00 & 16.46 & 13.10 & 13.50 \\
    \hline
    test\_1 & 15.87 & 4.12 & 1.99 & 4.70 & 5.06 \\
    \hline
    test\_2 & 34.65 & 13.74 & 6.76 & 6.83 & 7.31 \\
    \hline
    test\_3 & 15.54 & 3.82 & 1.89 & 4.74 & 5.09 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{All values above are normalized as ns/value.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.5\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260605-102501/overall_write.png}

  \vspace{0.2em}
  \begin{itemize}
    \item The \texttt{Delta2Vint} no-override case (\texttt{test\_2}) remains well below the baseline cost.
    \item The override-enabled cases (\texttt{test\_1} and \texttt{test\_3}) are cheaper still, consistent with the hypothesis that less logical data is being written.
  \end{itemize}
\end{frame}

\begin{frame}{Write Time : Spinloop (10{,}000 iter, 10{,}000~\textmu s)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Total & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 75.48 & 33.05 & 16.23 & 13.00 & 13.21 \\
    \hline
    test\_1 & 16.10 & 4.28 & 1.82 & 4.85 & 5.14 \\
    \hline
    test\_2 & 35.98 & 14.94 & 6.78 & 6.95 & 7.31 \\
    \hline
    test\_3 & 13.25 & 2.61 & 1.43 & 4.61 & 4.60 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{All values above are normalized as ns/value.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.5\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260603-162313/overall_write.png}

  \vspace{0.2em}
  \begin{itemize}
    \item The same ordering is preserved at the larger iteration count.
    \item This strengthens the claim that the write-overhead reduction in the override-enabled runs is structural rather than incidental.
  \end{itemize}
\end{frame}

\begin{frame}{Write Time : Chameleon POTRF ($n=65536$, $P=4$, 16 MPI jobs)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|}
    \hline
    Test & Total & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 86.97 & 36.51 & 17.98 & 15.95 & 16.53 \\
    \hline
    test\_1 & 23.43 & 6.12 & 2.93 & 7.00 & 7.38 \\
    \hline
    test\_2 & 49.36 & 20.82 & 8.89 & 9.43 & 10.21 \\
    \hline
    test\_3 & 23.21 & 5.83 & 2.98 & 7.03 & 7.37 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{All values above are normalized as ns/value.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.47\textheight,keepaspectratio]{../images/chameleon_stesting/run-analysis-20260604-125221/overall_write.png}

  \vspace{0.2em}
  \texttt{mpirun -np 16 --bind-to l3cache ./chameleon\_stesting -o potrf -H --niter 1 -w -P 4 -n 65536}
\end{frame}

\begin{frame}{Read-Time Benchmark Normalization}
  \small
  \begin{itemize}
    \item<1-> Read overhead also needs normalization, since different runs materialize different numbers of stored values and may replay different numbers of logical tokens.
    \item<2-> For the load phase, the benchmark measures the cost of forcing all stored vectors into memory through \texttt{load\_all\_data()}, which in turn triggers \texttt{codec->decode(...)} inside \texttt{load\_data(...)}.
    \item<3-> This load-side cost is therefore the part most directly affected by the chosen SubArray codec and its decode implementation.
    \item<4-> In \texttt{--both} mode, the benchmark also reports a replay phase: this is close to \texttt{pallas\_print}, except that it traverses the grammar and extracts token data without paying any \texttt{stdout} printing overhead.
    \item<5-> We normalize the overall read cost by a combined unit count:
    \[
      \texttt{total\_units} = \texttt{load\_total\_values} + \texttt{replay\_token\_occurrences},
    \]
    which yields the primary metric \texttt{total\_read\_ns\_per\_unit}.
    \item<6-> For replay-heavy spinloop traces, replay dominates the total read time; for Chameleon, the current benchmark run used load-only mode, so the reported read cost corresponds purely to codec-aware vector loading.
  \end{itemize}
\end{frame}

\begin{frame}{Read Time : Spinloop (1{,}000 iter, 10{,}000~\textmu s)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|r|}
    \hline
    Test & Total/Unit & Replay/Unit & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 1367.21 & 1354.05 & 5.51 & 2.47 & 2.12 & 2.12 \\
    \hline
    test\_1 & 1362.26 & 1350.24 & 4.03 & 1.99 & 2.57 & 2.56 \\
    \hline
    test\_2 & 1433.03 & 1420.39 & 4.79 & 2.37 & 2.26 & 2.27 \\
    \hline
    test\_3 & 1411.80 & 1399.41 & 4.20 & 1.99 & 2.71 & 2.59 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{Load-side values are normalized as ns/unit; replay is shown separately as replay ns/unit.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.5\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260605-102501/overall_read.png}

  \vspace{0.2em}
  \begin{itemize}
    \item Replay dominates the total read cost in this spinloop run, which is expected for a trace with a very large logical polling expansion.
    \item The remaining load-side contribution is where codec decode behavior is most directly visible.
  \end{itemize}
\end{frame}

\begin{frame}{Read Time : Spinloop (10{,}000 iter, 10{,}000~\textmu s)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|r|}
    \hline
    Test & Total/Unit & Replay/Unit & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 10380.29 & 10368.05 & 4.77 & 2.48 & 2.18 & 2.16 \\
    \hline
    test\_1 & 10120.42 & 10108.88 & 3.70 & 1.95 & 2.59 & 2.59 \\
    \hline
    test\_2 & 10810.26 & 10797.86 & 4.57 & 2.48 & 2.36 & 2.34 \\
    \hline
    test\_3 & 10056.54 & 10044.99 & 3.75 & 1.95 & 2.61 & 2.60 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{Load-side values are normalized as ns/unit; replay is shown separately as replay ns/unit.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.5\textheight,keepaspectratio]{../images/spinloop/run-analysis-20260603-162313/overall_read.png}

  \vspace{0.2em}
  \begin{itemize}
    \item The same dominance of replay persists at the larger iteration count.
    \item Once replay is separated out, the pure load-side codec overhead remains much smaller than the full logical replay cost.
  \end{itemize}
\end{frame}

\begin{frame}{Read Time : Chameleon POTRF ($n=65536$, $P=4$, 16 MPI jobs)}
  \tiny
  \centering
  \begin{tabular}{|c|r|r|r|r|r|r|}
    \hline
    Test & Total/Unit & Replay/Unit & Event TS & Seq TS & Seq Dur & Seq Excl Dur \\
    \hline
    test\_0 & 22.23 & 0.00 & 8.20 & 4.36 & 3.85 & 3.83 \\
    \hline
    test\_1 & 20.66 & 0.00 & 6.48 & 3.45 & 4.37 & 4.37 \\
    \hline
    test\_2 & 23.30 & 0.00 & 8.55 & 4.49 & 4.15 & 4.11 \\
    \hline
    test\_3 & 20.57 & 0.00 & 6.45 & 3.46 & 4.36 & 4.36 \\
    \hline
  \end{tabular}

  \vspace{0.2em}
  \texttt{This Chameleon campaign used load-only mode, so replay cost is zero by construction.}

  \vspace{0.3em}
  \includegraphics[width=\textwidth,height=0.47\textheight,keepaspectratio]{../images/chameleon_stesting/run-analysis-20260604-125221/overall_read.png}

  \vspace{0.2em}
  \texttt{mpirun -np 16 --bind-to l3cache ./chameleon\_stesting -o potrf -H --niter 1 -w -P 4 -n 65536}
\end{frame}



\end{document}
